# 구독 플랜 UI 테스트 가이드 (DP-495)

백엔드 개발자가 Phase 5 · 6 · 7 UI를 직접 확인하기 위한 가이드입니다.

---

## 실행 방법

```bash
# 1. 의존성 설치
npm install

# 2. 개발 서버 실행 (포트 3000)
npm run dev
```

브라우저에서 `http://localhost:3000` 접속 후 로그인하세요.  
(API 기본값은 운영 서버 `https://3-39-96-126.sslip.io`를 바라봅니다.)

---

## 테스트 페이지 진입

로그인 후 아래 주소로 직접 이동하세요.

```
http://localhost:3000/dev/subscription-test
```

이 페이지에서 구독 시나리오를 버튼 하나로 바꿀 수 있습니다.  
**실제 API 호출 없이** Zustand 전역 상태를 직접 변경하는 방식이라 백엔드 연동 전에도 UI를 그대로 확인할 수 있습니다.

> **중요**: 테스트 페이지에서 시나리오를 바꾼 뒤, 상단에 표시된 링크나 직접 URL을 입력해 해당 페이지로 이동하면 됩니다. 페이지를 새로 고침하면 상태가 초기화되므로, 새 탭을 열거나 뒤로가기로 테스트 페이지로 돌아오세요.

---

## Phase별 확인 항목

### Phase 1 · 2 · 3 · 4 (기반 / 플랜 페이지 / 결제 / 공통 모달)

시각적으로 확인 가능한 항목들입니다.

| 확인 항목                                     | 경로                           |
| --------------------------------------------- | ------------------------------ |
| 플랜 소개 페이지 (Free / Pro / Max 비교 카드) | `/plans`                       |
| TopNav 프로필 드롭다운 → "구독" 메뉴          | 우상단 프로필 클릭             |
| 카드 등록 페이지 (토스페이먼츠 위젯)          | `/payment/billing?plan=PRO`    |
| 결제 완료 페이지                              | `/payment/success` (직접 이동) |
| 결제 실패 페이지                              | `/payment/fail` (직접 이동)    |

> **결제 플로우 주의**: 현재 `/subscriptions/billing-auth`, `/subscriptions` (해지), `/subscriptions/cancel` (환불) API는 **mock 반환**입니다. 실제 토스페이먼츠 카드 등록 화면은 `NEXT_PUBLIC_TOSS_CLIENT_KEY` 환경변수 설정이 필요합니다 (테스트 키는 백엔드에서 받아 프로젝트 루트의 `.env.local`에 추가).

---

### Phase 5 — 프로필 구독 관리 섹션

**확인 위치**: 테스트 페이지 상단 **"Phase 5"** 구역 또는 `/profile`

#### 시나리오별 확인 포인트

| 버튼                              | 확인할 내용                                                                                                  |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| **FREE (limits 없음 — 스켈레톤)** | 이번 주 사용량 항목이 스켈레톤으로 표시되고, "사용량 정보를 불러오는 중이에요." 안내 문구 노출               |
| **FREE (limits 있음)**            | `[Free]` 배지 · 4개 사용량 프로그레스바 · "Pro/Max로 업그레이드" 버튼                                        |
| **PRO — 정기갱신 중**             | `[PRO]` 배지 · 다음 결제일 · "Max 플랜으로 업그레이드" + "구독 해지" 버튼 · **환불 링크 없음** (결제 10일째) |
| **PRO — 정기갱신 + 환불 가능**    | "결제 취소 및 환불" 링크 추가 노출 (결제 3일째 → 7일 이내)                                                   |
| **PRO — 해지 예정**               | "N월 N일까지 이용 가능 (해지 예정)" + "다시 구독하기" 버튼                                                   |
| **MAX — 정기갱신 중**             | `[MAX]` 배지 · 4개 항목 전부 "무제한"                                                                        |
| **PRO — Max로 변경 예약됨**       | "다음 결제부터 MAX로 변경됩니다 (변경일: N월 N일)" + [변경 취소] 버튼                                        |
| **MAX — Pro로 변경 예약됨**       | "다음 결제부터 PRO로 변경됩니다 (변경일: N월 N일)" + [변경 취소] 버튼                                        |

#### 구독 관련 버튼 동작 (현재 mock)

- **구독 해지**: 확인 모달 → 해지 성공 토스트 → "해지 예정" 상태로 즉시 전환
- **환불**: 확인 모달 → 환불 성공 토스트 → FREE 상태로 즉시 전환
- **변경 취소**: [변경 취소] 클릭 → `pendingPlanType` null 초기화 → 예약 UI 사라짐

> 이 버튼들은 현재 실제 API를 호출하지만 **mock 응답**을 반환합니다. 백엔드 API 완성 후 Phase 8에서 실제 연동합니다.

---

### Phase 6 — 기존 페이지 플랜 제한 UI

**확인 위치**: 테스트 페이지 중간 **"Phase 6"** 구역

#### 시나리오 적용 순서

1. 테스트 페이지에서 아래 시나리오 버튼 중 하나를 클릭
2. 표시된 링크 카드 또는 직접 URL로 이동해서 확인

| 시나리오 버튼               | 적용되는 상태                                 |
| --------------------------- | --------------------------------------------- |
| FREE · JUNIOR · limits 있음 | planType=FREE, level=JUNIOR, aiDaily 3회 남음 |
| FREE · AI 개선 소진         | aiDaily remaining=0                           |
| PRO · MIDDLE · limits 있음  | planType=PRO, 전 레벨 해제                    |
| MAX · limits 무제한         | remaining=-1 전체                             |

#### 페이지별 확인 항목

**홈 피드** (`/home`)

- FREE 유저가 콘텐츠 50개 한도에 도달하면 피드 하단에 업그레이드 배너 표시
- 백엔드 연동 전: `/contents` 응답에 `planLimited` 필드 없으면 배너 미표시 (정상)

**홈 글 상세** (`/home/{글ID}`)

- AI 요약 탭: FREE 유저는 본인 레벨(JUNIOR면 JUNIOR만) 외 탭에 🔒 아이콘 + 클릭 시 업그레이드 모달
- AI 퀴즈 탭: 동일 패턴

**커뮤니티 글쓰기** (`/community/write`)

- AI 질문 개선 패널: 남은 횟수 표시 / `remaining=0`이면 버튼 비활성화 + 초기화 시각 표시
- "게시하기" 클릭 시 팝업: "AI 답변을 함께 생성할까요?" + 남은 횟수 표시
  - `remaining=0`이면 AI 포함 버튼 비활성화

**주간 리포트** (`/report`)

- `locked: true` 항목: 🔒 아이콘 + 클릭 시 업그레이드 모달
- 백엔드 연동 전: `locked` 필드 없으면 전부 클릭 가능 (정상)

**플랜 소개** (`/plans`)

- FREE 유저: 업그레이드 버튼 → `/payment/billing?plan=PRO` 이동
- PRO/MAX 유저: 업그레이드 버튼 → `POST /subscriptions/change` 직접 호출 (mock)

---

### Phase 7 — 채용 기능 limits UI

**확인 위치**: 테스트 페이지 하단 **"Phase 7"** 구역

#### 시나리오 적용 → 공고 상세 이동

1. 시나리오 버튼 클릭 (상태 현황이 바로 위에 표시됨)
2. 공고 ID 입력란에 실제 공고 ID 입력 → "공고 상세로 이동" 클릭
3. `/jobs/{공고ID}` 에서 3개 섹션 확인

#### 공고 상세 페이지 확인 섹션

**부족 역량 보완 섹션** (skillBoostWeekly)

| 시나리오                | 보여야 하는 UI                                                                              |
| ----------------------- | ------------------------------------------------------------------------------------------- |
| FREE · 각 기능 1회 남음 | 섹션 제목 옆 "이번 주 1회 남음" · 버튼 활성화                                               |
| FREE · 부족역량 소진    | 섹션 제목 옆 "이번 주 횟수를 모두 사용했어요 · 다음 월요일 초기화" (빨간색) · 버튼 비활성화 |
| PRO · 5/7회 남음        | 섹션 제목 옆 "이번 주 5회 남음"                                                             |
| MAX · 무제한            | 섹션 제목 옆 "무제한"                                                                       |
| limits 없음             | 섹션 제목 옆 아무것도 없음                                                                  |

**예상 면접 Q&A 섹션** (interviewQaGenerateWeekly)

| 시나리오                | 보여야 하는 UI                                                |
| ----------------------- | ------------------------------------------------------------- |
| FREE · 각 기능 1회 남음 | 제목 옆 "이번 주 2회 남음" · "면접 질문 생성하기" 버튼 활성화 |
| FREE · 면접 Q&A 소진    | 제목 옆 소진 문구 (빨간색) · 생성/재생성 버튼 비활성화        |
| MAX · 무제한            | "무제한"                                                      |

**AI 모의면접 섹션** (mockInterviewWeekly)

| 시나리오                | 보여야 하는 UI                                           |
| ----------------------- | -------------------------------------------------------- |
| FREE · 각 기능 1회 남음 | 제목 옆 "이번 주 2회 남음" · "모의면접 시작" 버튼 활성화 |
| FREE · 모의면접 소진    | 제목 옆 소진 문구 (빨간색) · 버튼 비활성화               |
| MAX · 무제한            | "무제한"                                                 |

#### 이력서 페이지 탭 확인

`/my-resume` 접속 후 탭별 확인

| 탭              | 확인 항목                                                                  |
| --------------- | -------------------------------------------------------------------------- |
| **모의면접 탭** | "새 모의면접" 버튼 위/옆에 남은 횟수 표시. `remaining=0`이면 버튼 비활성화 |
| **Q&A 탭**      | Q&A 선택 시 우측 상단 (PDF/삭제 버튼 위)에 면접 Q&A 생성 남은 횟수 표시    |

---

## 백엔드 연동 시 추가 확인 사항 (Phase 8)

실제 API 연동 후 `/users/me` 응답에 아래 필드가 포함되어야 UI가 정상 동작합니다.

```json
{
  "planType": "FREE",
  "planExpiredAt": null,
  "lastBilledAt": null,
  "pendingPlanType": null,
  "limits": {
    "aiDaily": {
      "used": 2,
      "max": 5,
      "remaining": 3,
      "resetsAt": "2026-05-22T00:00:00Z"
    },
    "skillBoostWeekly": {
      "used": 1,
      "max": 2,
      "remaining": 1,
      "resetsAt": "2026-05-25T00:00:00Z"
    },
    "interviewQaGenerateWeekly": {
      "used": 0,
      "max": 2,
      "remaining": 2,
      "resetsAt": "2026-05-25T00:00:00Z"
    },
    "mockInterviewWeekly": {
      "used": 0,
      "max": 2,
      "remaining": 2,
      "resetsAt": "2026-05-25T00:00:00Z"
    }
  }
}
```

- `remaining: -1` = 무제한 (MAX 플랜)
- `resetsAt: null` = MAX 플랜 (리셋 없음)
- `limits` 필드 자체가 없으면 → UI에서 사용량 섹션 숨김 / limits 문구 미표시 (방어 처리 완료)

구독 관련 API mock → 실제 연동 교체 대상:

- `POST /subscriptions/billing-auth`
- `DELETE /subscriptions` (해지)
- `POST /subscriptions/cancel` (환불)
- `POST /subscriptions/change` (플랜 변경 예약)
